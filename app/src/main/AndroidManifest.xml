<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_avatar_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_avatar_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Study_app"
        tools:targetApi="31">
        <activity
            android:name=".ui.Timetable.TimetableWidget"
            android:exported="false" />

        <receiver
            android:name=".ui.Deadline.DeadlineNotificationReceiver"
            android:enabled="true"
            android:exported="false" />
        <receiver
            android:name=".ui.Timetable.SubjectNotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Study_app.NoActionBar" />
        <activity android:name=".ui.Deadline.MainDeadLineMonHoc" />
        <activity android:name=".ui.Deadline.MainDeadLine" />
        <activity android:name=".ui.Deadline.InputDeadlineActivity" />
        <activity android:name=".ui.Deadline.InfoDeadlineActivity" />
        <activity android:name=".ui.Subject.SubjectListActivity" />
        <activity android:name=".ui.Subject.SubjectAddActivity" />
        <activity android:name=".ui.Subject.SubjectDetailActivity" />
        <activity android:name=".ui.Subject.SubjectBulkImportActivity" />
        <activity android:name=".ui.Score.InputScoreActivity" />
        <activity android:name=".ui.Score.ScoreSummaryActivity" />
        <activity android:name=".ui.Notes.NotesActivity" />
        <activity android:name=".ui.Notes.InputNoteActivity" />
        <activity android:name=".ui.Curriculum.CurriculumActivity" />
        <activity
            android:name=".ui.Timetable.TimetableWeek"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

<!--        widget-->
        <receiver
            android:name=".widget.TimetableWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/timetable_widget_info" />
        </receiver>

    </application>

</manifest>