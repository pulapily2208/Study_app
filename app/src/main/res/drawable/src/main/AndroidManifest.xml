<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_avatar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_avatar_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Study_app">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.Deadline.MainDeadLineMonHoc"/>
        <activity
            android:name=".ui.Deadline.InputDeadlineActivity"/>
        <activity android:name=".ui.Subject.SubjectListActivity"/>
        <activity android:name=".ui.Subject.SubjectAddActivity"/>
        <activity android:name=".ui.Subject.SubjectDetailActivity"/>
        <activity android:name=".ui.Notes.NotesActivity" />
        <activity android:name=".ui.Notes.InputNoteActivity" />
        <activity android:name=".ui.Notes.NotesDetailActivity" />
        <activity android:name=".ui.Curriculum.CurriculumActivity" />


        <activity android:name=".ui.Timetable.TimetableWeek" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>